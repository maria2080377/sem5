#include <iostream>    //Задача 1
#include <vector> 
#include <algorithm>
#include <numeric>
using namespace std;
int main() {
	setlocale(LC_ALL, "RU");
	vector <int> vec(10);
	iota(vec.begin(), vec.end(), 1);
	for_each(vec.begin(), vec.end(), [](int x) {cout << x<<" "; });

	return 0;
}
